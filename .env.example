# API base URL for mock server
API_BASE_URL=http://localhost:4000

# Hardcoded IDs — stand-ins for real auth tokens in production.
# In a real app these would come from the auth session, not a .env file.
# Resolved ambiguity per README §5.4: placed in constants rather than inline in screens.
CUSTOMER_ID=c-001
DRIVER_ID=d-101
ADMIN_ID=a-201
